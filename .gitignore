# Raw source media (keep locally, don't commit)
Videos/Hero section.mov
Videos/mobileHero.mov

# OS / editor junk
.DS_Store
Thumbs.db
*.log
.vscode/*
!.vscode/extensions.json

# Migration working files (DB exports, generated SQL - contain signed storage URLs)
backups/
sql/
